apiVersion: 1

providers:
  - name: 'Signal Lab'
    orgId: 1
    folder: 'Signal Lab'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
